# URL of the deployed Cloudflare Worker proxy (see /worker).
# After `wrangler deploy` this looks like:
# https://kursi-tojik-proxy.<your-subdomain>.workers.dev
VITE_WORKER_URL=https://kursi-tojik-proxy.YOUR-SUBDOMAIN.workers.dev
